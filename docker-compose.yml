services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000" # put nginx/caddy in front of this for HTTPS
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
